#version = "1.0"
#version = "1.1" #added console % readiness. Improved speed.
#version = "1.2" # Moved logger, Version control and config parser to separate files
version = "1.3" # added checked file count to log and file extension is case-insensitive.